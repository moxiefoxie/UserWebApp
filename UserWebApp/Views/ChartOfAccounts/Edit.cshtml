@model UserWebApp.Models.ChartOfAccount

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>ChartOfAccount</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.AccountNumber)

        @Html.HiddenFor(model => model.AccoutnDesc)

        @Html.HiddenFor(model => model.NormalSide)

        @Html.HiddenFor(model => model.Category)

        @Html.HiddenFor(model => model.Subcategory)

        @Html.HiddenFor(model => model.InitialBalance)

        @Html.HiddenFor(model => model.Debit)

        @Html.HiddenFor(model => model.Credit)

        @Html.HiddenFor(model => model.DateCreated)

        @Html.HiddenFor(model => model.Balance)

        @Html.HiddenFor(model => model.UserID)

        @Html.HiddenFor(model => model.Order)

        @Html.HiddenFor(model => model.Statement)

        @Html.HiddenFor(model => model.Comment)

        @Html.HiddenFor(model => model.AccountName)

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
